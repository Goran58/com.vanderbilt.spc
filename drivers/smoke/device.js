"use strict";

// Vanderbilt SPC Smoke Sensor Device

const Homey = require('homey');
const SensorDevice = require('../sensor_device.js');

class SmokeDevice extends SensorDevice {
}

module.exports = SmokeDevice;
